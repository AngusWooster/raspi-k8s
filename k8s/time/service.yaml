apiVersion: v1
kind: Service
metadata:
  name: time-daemon
  namespace: aku-sw
spec:
  type: NodePort
  selector:
    app: time-daemon
  ports:
    - protocol: TCP
      port: 50052
      targetPort: 50052
      nodePort: 30052
